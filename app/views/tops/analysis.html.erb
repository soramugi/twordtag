<% title '形態素解析' %>

<div class="col-lg-12">
  <h2>形態素解析</h2>
  <p>文章から抜き出される単語の確認が出来ます</p>
</div>
<div class="row">
  <div class="col-xs-6">
    <div class="well">
      <div class="form-group">
        <%= label_tag '解析文字の入力' %>
        <%= text_area :analysis, :text, {placeholder: @str, id: 'textArea', class: 'form-control', rows: 15} %>
      </div>
      <div class="form-group">
        <%= submit_tag '解析', { class: 'btn btn-default', id: 'analysis-btn'} %>
      </div>
    </div>
  </div>
  <div class="col-xs-6">
    <div id="preview" class="well">
      <ul class="unstyled">
        <% @words.each do |word,count| %>
          <li><%= count.to_s + ' ' + word %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
